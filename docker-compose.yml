# Single-container-drift. Bygger image lokalt från Dockerfile och serverar
# appen statiskt med nginx. Justera host-porten vid behov.
services:
  app:
    build: .
    image: hrplus-lon:local
    container_name: hrplus-lon
    ports:
      - "8848:80"
    restart: unless-stopped
